<p id="notice"><%= notice %></p>

<h1>Shops</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shops.each do |shop| %>
      <tr>
        <td><%= link_to 'Ouvrir', shop_path(shop.slug) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
